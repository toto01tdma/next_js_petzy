# Create .env.local file

# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Set to 'true' to enable API calls, 'false' for preview mode (UI only)
NEXT_PUBLIC_USE_API_MODE=false
